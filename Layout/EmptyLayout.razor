@* Every screen in this app (splash/login/register/dashboard) is a fully
   custom full-bleed design per the Figma mockups, so the global layout
   just renders the page - no shared chrome, top bar, or side nav here. *@
@inherits LayoutComponentBase

@Body
